# 八字命理分析平台 - 完整实施计划

## 📊 项目概述

基于传统八字命理的现代化Web应用，支持用户注册、付费查看、管理后台编辑等完整功能。

---

## 🎯 功能模块分解

### 模块 1️⃣：用户认证系统

**优先级：高** | **预计工时：4-6小时**

#### 需要实现的文件：

1. **API Routes**
   - `/app/api/auth/register/route.ts` - 用户注册
   - `/app/api/auth/login/route.ts` - 用户登录
   - `/app/api/auth/me/route.ts` - 获取当前用户信息

2. **前端页面**
   - `/app/login/page.tsx` - 登录页面
   - `/app/register/page.tsx` - 注册页面

3. **工具函数**
   - `/lib/auth.ts` - 认证工具（JWT token、密码验证）
   - `/lib/session.ts` - Session管理

#### 功能细节：
- ✅ 手机号格式验证（11位数字）
- ✅ 密码加密存储（bcrypt）
- ✅ JWT Token生成和验证
- ✅ Session持久化（localStorage或cookie）
- ✅ 登录状态保护

---

### 模块 2️⃣：八字计算与报告生成

**优先级：最高** | **预计工时：6-8小时**

#### 需要实现的文件：

1. **工具函数**
   - ✅ `/lib/bazi.ts` - 八字计算（已完成）
   - `/lib/report-generator.ts` - 按模板生成报告内容
   - `/lib/cities.ts` - 中国城市经纬度数据

2. **API Routes**
   - `/app/api/reports/create/route.ts` - 创建报告（集成八字计算）
   - 更新 `/app/api/reports/[id]/route.ts` - 支持新字段

3. **前端页面**
   - 更新 `/app/calc/page.tsx` - 新增姓名、国家、城市选择、券码输入
   - 新增城市自动补全组件

#### 功能细节：
- ✅ 真太阳时计算（基于城市经纬度）
- ✅ 完整八字排盘（年月日时四柱）
- ✅ 五行分析
- ✅ 按模板自动生成8页报告内容
- ✅ 城市联想输入（支持拼音搜索）
- ✅ 国家选择（中国/其他）

---

### 模块 3️⃣：券码系统

**优先级：高** | **预计工时：3-4小时**

#### 需要实现的文件：

1. **API Routes**
   - `/app/api/vouchers/generate/route.ts` - 批量生成券码
   - `/app/api/vouchers/verify/route.ts` - 验证券码
   - `/app/api/vouchers/list/route.ts` - 券码列表（管理员）

2. **工具函数**
   - `/lib/voucher.ts` - 券码生成工具（30字符随机）

3. **前端页面**
   - `/app/admin/vouchers/page.tsx` - 券码管理页面
   - 更新 `/app/reports/[id]/page.tsx` - 券码验证逻辑

#### 功能细节：
- ✅ 生成30字符随机券码
- ✅ 一次生成50个券码
- ✅ 券码使用状态追踪
- ✅ 一个券码对应一个报告
- ✅ 券码过期机制（可选）

---

### 模块 4️⃣：报告查看权限控制

**优先级：高** | **预计工时：2-3小时**

#### 需要更新的文件：

1. **前端页面**
   - 更新 `/app/reports/[id]/page.tsx`
     - 检查用户登录状态
     - 检查报告所属权限
     - 券码验证弹窗

2. **API Routes**
   - 更新 `/app/api/reports/[id]/route.ts`
     - 添加权限验证

#### 功能细节：
- ✅ 用户只能查看自己的报告
- ✅ 未输入券码的报告，查看时弹窗要求输入券码
- ✅ 券码验证成功后，报告与券码绑定
- ✅ 已发布的报告才显示完整内容，草稿显示"分析中"

---

### 模块 5️⃣：管理后台完善

**优先级：中** | **预计工时：4-5小时**

#### 需要实现的文件：

1. **前端页面**
   - `/app/admin/page.tsx` - 管理后台首页（报告列表）
   - `/app/admin/reports/page.tsx` - 所有报告列表
   - 更新 `/app/admin/reports/[id]/page.tsx` - 显示八字详情

2. **API Routes**
   - `/app/api/admin/reports/route.ts` - 获取所有报告列表
   - `/app/api/admin/stats/route.ts` - 统计数据

#### 功能细节：
- ✅ 报告列表（分页、搜索、筛选）
- ✅ 显示用户信息、八字信息
- ✅ 编辑器内显示：
  - 完整八字（年月日时柱）
  - 五行分析
  - 真太阳时
  - 用户姓名、出生信息
- ✅ 统计面板（总报告数、已发布数、用户数）

---

### 模块 6️⃣：城市经纬度数据

**优先级：中** | **预计工时：2-3小时**

#### 需要实现的文件：

1. **数据文件**
   - `/data/cities.json` - 中国城市经纬度数据（~300+城市）

2. **工具函数**
   - `/lib/cities.ts` - 城市搜索、拼音匹配

3. **前端组件**
   - `/components/CityAutocomplete.tsx` - 城市自动补全组件

#### 功能细节：
- ✅ 支持拼音搜索（如"beijing"匹配"北京"）
- ✅ 支持汉字搜索
- ✅ 返回城市经纬度
- ✅ 只有中国需要城市，其他国家使用标准时间

---

### 模块 7️⃣：移动端适配

**优先级：中** | **预计工时：3-4小时**

#### 需要更新的文件：

1. **全局样式**
   - 更新 `/app/globals.css`
   - 添加移动端断点样式

2. **所有页面组件**
   - 响应式布局调整
   - 触摸友好的交互

#### 功能细节：
- ✅ 断点设计：sm(640px), md(768px), lg(1024px)
- ✅ 移动端导航菜单
- ✅ 表单输入优化（大按钮、合适间距）
- ✅ 触摸滑动优化

---

### 模块 8️⃣：UI/UX 优化

**优先级：低** | **预计工时：3-4小时**

#### 优化内容：

1. **颜色系统**
   - 低饱和度配色方案
   - Apple风格渐变
   - 暗色模式（可选）

2. **动画效果**
   - 页面过渡动画
   - 加载动画
   - 微交互反馈

3. **组件库**
   - 统一按钮样式
   - 统一表单样式
   - 统一卡片样式

---

## 🚀 MVP 核心功能建议

### 方案 A：最小可用版本（6-8小时）

**核心功能：**
1. ✅ 八字计算与报告生成（模块2）
   - 姓名、性别、生日、城市输入
   - 真太阳时计算
   - 按模板生成完整报告
2. ✅ 简化的管理后台（模块5部分功能）
   - 报告列表
   - 编辑并发布报告

**暂不实现：**
- ❌ 用户登录系统
- ❌ 券码系统
- ❌ 权限控制
- ❌ 移动端适配

**适用场景：** 快速验证核心价值，单管理员使用

---

### 方案 B：付费版本（12-15小时）

**核心功能：**
1. ✅ 八字计算与报告生成（模块2）
2. ✅ 券码系统（模块3）
3. ✅ 报告权限控制（模块4）
4. ✅ 简化的管理后台（模块5部分）

**暂不实现：**
- ❌ 用户登录注册（使用临时ID或仅券码验证）
- ❌ 移动端完整适配
- ❌ 高级UI优化

**适用场景：** 可以开始收费运营，但用户体验简化

---

### 方案 C：完整多用户版本（20-25小时）

**核心功能：**
1. ✅ 模块1-5 全部实现
2. ✅ 基础移动端适配

**适用场景：** 正式上线运营，支持多用户

---

## 📝 技术实施细节

### 城市数据结构示例
```json
{
  "cities": [
    {
      "name": "北京",
      "pinyin": "beijing",
      "province": "北京",
      "longitude": 116.4074,
      "latitude": 39.9042
    }
  ]
}
```

### 报告模板变量映射
```typescript
{
  report_id: string;
  birth_date: string;
  birth_time: string;
  gender: '男' | '女';
  location: string;
  today: string;
  year_gz: string;  // 年柱干支
  month_gz: string; // 月柱干支
  day_gz: string;   // 日柱干支
  hour_gz: string;  // 时柱干支
  wood: number;     // 木%
  fire: number;     // 火%
  earth: number;    // 土%
  metal: number;    // 金%
  water: number;    // 水%
  element_dominant: string;
  element_symbolism: string;
  element_weak: string;
  element_weak_area: string;
  balance_action: string;
  // ... 更多模板变量
}
```

---

## 💾 数据库迁移注意事项

- ✅ 已完成：User、Voucher、Report 表创建
- ⚠️ 注意：每次 schema 变更需要运行 `npm run db:push`
- 💡 建议：开发阶段使用 SQLite，生产环境考虑 PostgreSQL

---

## 🔒 安全注意事项

1. **密码安全**
   - ✅ 使用 bcrypt 加密
   - ✅ 最小长度6位
   - ⚠️ 生产环境建议8位+特殊字符

2. **API 安全**
   - ⚠️ 需要添加：Rate limiting
   - ⚠️ 需要添加：CORS 配置
   - ⚠️ 需要添加：Input validation

3. **券码安全**
   - ✅ 30字符随机生成
   - ⚠️ 建议添加：过期时间
   - ⚠️ 建议添加：使用次数限制

---

## 📊 预计总工时

| 方案 | 核心功能 | 预计工时 | 可用性 |
|------|---------|---------|--------|
| A - MVP | 八字计算+简单后台 | 6-8小时 | 单管理员演示 |
| B - 付费版 | +券码系统 | 12-15小时 | 可收费运营 |
| C - 完整版 | +用户系统 | 20-25小时 | 正式产品 |

---

## 🎯 下一步建议

**现在请选择一个方案：**

1. **方案 A** - 我们先实现核心的八字计算和报告生成，快速看到效果
2. **方案 B** - 实现付费功能，可以开始运营
3. **方案 C** - 完整实现，一次到位

**或者自定义组合** - 告诉我您最需要哪些功能，我们定制一个方案

---

## 📦 已完成的工作

✅ 数据库设计（User、Voucher、Report）
✅ 八字计算工具 (`/lib/bazi.ts`)
✅ 依赖安装（lunar-javascript, bcryptjs, react-markdown）
✅ 基础项目结构

---

生成时间：2025-01-08
