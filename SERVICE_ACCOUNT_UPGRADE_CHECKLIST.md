# 服务号升级 + 模板消息配置完整清单

## 📋 整体流程

```
订阅号 → 升级到服务号 → 准备自定义域名 → 绑定域名到云托管 → 配置微信服务器 → 测试验证
```

---

## 第一步：升级到服务号

### 方式1：账号迁移（推荐）

**适用场景**: 已有订阅号，想保留粉丝和历史内容

**操作步骤**:
1. 登录[微信公众平台](https://mp.weixin.qq.com)
2. 进入"设置与开发" → "公众号设置" → "账号迁移"
3. 选择"迁移到服务号"
4. 准备材料：
   - 企业营业执照（必须是企业/组织，个人无法升级）
   - 对公账户信息
   - 管理员身份证
   - 迁移函（微信提供模板）
5. 提交审核，等待微信人工审核（约3-7天）
6. 审核通过后，原订阅号自动变为服务号

**费用**: 通常需要支付300元认证费（如果原订阅号已认证，可能免除）

**注意事项**:
- ✅ 保留所有粉丝
- ✅ 保留历史内容
- ❌ 群发次数从每天1次变为每月4次
- ❌ 消息不再折叠，但默认免打扰

---

### 方式2：重新申请服务号

**适用场景**: 订阅号粉丝少，可以放弃

**操作步骤**:
1. 重新注册一个新的微信公众号
2. 选择"服务号"类型
3. 填写企业/组织信息
4. 完成认证（需支付300元认证费）

**优点**: 流程简单，时间较快（1-2天）
**缺点**: 丢失原订阅号的粉丝和内容

---

## 第二步：准备自定义域名

### 域名要求

- ✅ **已备案**（必须完成ICP备案）
- ✅ 支持添加DNS解析
- ✅ 推荐使用二级域名（如 `api.yourdomain.com`）

### 域名购买渠道

| 平台 | .com域名价格 | .cn域名价格 | 备案支持 |
|------|------------|------------|---------|
| 阿里云 | 约55-78元/年 | 约29元/年 | ✅ 免费 |
| 腾讯云 | 约55元/年 | 约35元/年 | ✅ 免费 |
| 百度云 | 约60元/年 | 约30元/年 | ✅ 免费 |

### 备案流程（如果域名未备案）

1. 在域名服务商提交ICP备案申请
2. 准备材料：
   - 营业执照
   - 法人身份证
   - 网站负责人身份证
   - 网站备案信息表
3. 提交后等待管局审核（约7-20天）
4. 备案通过后获得备案号

**注意**: 域名必须先备案才能绑定到云托管服务

---

## 第三步：绑定域名到微信云托管

### 操作步骤

1. **登录微信云托管控制台**
   - 访问 https://cloud.weixin.qq.com
   - 选择你的服务 `zhibaitang-bazisever-207188-4-1391586262`

2. **添加自定义域名**
   - 进入"服务配置" → "域名管理"
   - 点击"添加域名"
   - 输入你的域名，例如 `api.yourdomain.com`
   - 点击"确定"

3. **获取CNAME记录**
   - 系统会显示一个CNAME值，类似：
     ```
     zhibaitang-bazisever-207188-4-1391586262.sh.run.tcloudbase.com
     ```
   - 复制这个值

4. **配置DNS解析**
   - 登录你的域名服务商（阿里云/腾讯云等）
   - 进入DNS解析管理
   - 添加CNAME记录：
     ```
     主机记录: api（或你选择的子域名）
     记录类型: CNAME
     记录值: zhibaitang-bazisever-207188-4-1391586262.sh.run.tcloudbase.com
     TTL: 600（或默认值）
     ```
   - 保存配置

5. **等待生效**
   - DNS解析通常需要10-30分钟生效
   - 可以使用以下命令检查：
     ```bash
     nslookup api.yourdomain.com
     ```
   - 如果返回 `.tcloudbase.com` 的IP地址，说明已生效

6. **云托管验证**
   - 回到云托管控制台
   - 点击"验证域名"
   - 验证通过后，域名状态变为"已启用"

---

## 第四步：测试自定义域名

### 验证域名可访问性

```bash
# 替换为你的实际域名
curl "https://api.yourdomain.com/api/wechat/callback?signature=test&timestamp=1234567890&nonce=test&echostr=hello"
```

**预期结果**: 返回 "Invalid signature"（403错误）

如果返回500错误或无法访问，说明域名配置有问题。

### 验证签名逻辑

```bash
# 计算正确签名并测试
node -e "
const crypto = require('crypto');
const TOKEN = 'bazi_life_2025_secure_token';
const timestamp = '1234567890';
const nonce = 'test_nonce';
const echostr = 'hello_world_test';

const arr = [TOKEN, timestamp, nonce].sort();
const str = arr.join('');
const signature = crypto.createHash('sha1').update(str).digest('hex');

const url = \`https://api.yourdomain.com/api/wechat/callback?signature=\${signature}&timestamp=\${timestamp}&nonce=\${nonce}&echostr=\${echostr}\`;

fetch(url).then(res => res.text()).then(body => {
  if (body === echostr) {
    console.log('✅ 验证成功！');
  } else {
    console.log('❌ 验证失败:', body);
  }
});
"
```

**预期结果**: 输出 `✅ 验证成功！`

---

## 第五步：配置微信公众号服务器

### 操作步骤

1. **登录微信公众平台**
   - 访问 https://mp.weixin.qq.com
   - 使用服务号账号登录

2. **进入服务器配置**
   - 左侧菜单："设置与开发" → "基本配置"
   - 找到"服务器配置"板块
   - 点击"修改配置"

3. **填写配置信息**
   ```
   服务器地址(URL): https://api.yourdomain.com/api/wechat/callback
   Token: bazi_life_2025_secure_token
   EncodingAESKey: （点击"随机生成"）
   消息加解密方式: 明文模式
   数据格式: XML
   ```

4. **提交验证**
   - 点击"提交"
   - 微信会发送GET请求验证你的服务器
   - 如果配置正确，会显示"提交成功"

5. **启用服务器配置**
   - 验证成功后，点击"启用"
   - 服务器配置状态变为"已启用"

---

## 第六步：测试关注事件推送

### 测试流程

1. **用微信扫码关注服务号**
   - 使用一个测试微信账号
   - 确保该账号已登录过小程序（有unionid）

2. **检查后端日志**
   - 登录微信云托管控制台
   - 查看实时日志
   - 应该看到类似日志：
     ```
     📝 收到微信服务器验证请求
     ✅ 签名验证成功
     📨 收到微信事件推送: <xml>...subscribe...</xml>
     👤 用户关注事件: openid=oXXXXX...
     ✅ 用户绑定成功: userId=cmXXXXX...
     ```

3. **检查数据库**
   ```bash
   # 查询用户表，确认mpOpenid和subscribeStatus已更新
   npx prisma studio
   ```
   - 打开User表
   - 找到测试用户
   - 确认 `mpOpenid` 已填充
   - 确认 `subscribeStatus` 为 `true`

---

## 第七步：测试模板消息推送

### 准备工作

1. **确认模板消息已添加**
   - 微信公众平台："广告与服务" → "模板消息"
   - 确认模板ID: `1t1da7scGa8z8719HnX9OJdVa_YDzuBLUbvkOwFpKRg` 已启用

2. **创建测试报告**
   - 在小程序中提交一个测算请求
   - 或在数据库中手动创建一条 `draft` 状态的报告
   - 确保该报告关联的用户已关注公众号

### 测试推送

1. **在管理后台发布报告**
   - 访问管理后台
   - 找到测试报告
   - 点击"发布报告"（将状态从 `draft` 改为 `published`）

2. **检查推送结果**
   - 查看云托管日志，应该看到：
     ```
     📢 触发公众号消息推送
     ✅ 模板消息发送成功
     ```
   - 测试微信账号应该收到公众号消息通知
   - 点击消息应该跳转到小程序报告详情页

---

## 常见问题排查

### Q1: 域名绑定后提示"域名未备案"

**原因**: 域名未完成ICP备案
**解决**: 先完成域名备案，再绑定到云托管

---

### Q2: 微信配置提交后报错"请求URL超时"

**可能原因**:
1. 云托管服务未运行
2. 防火墙拦截微信服务器IP
3. HTTPS证书问题

**排查步骤**:
```bash
# 1. 测试域名可访问性
curl -I https://api.yourdomain.com/api/wechat/callback

# 2. 查看云托管服务状态
# 登录云托管控制台，确认服务正在运行

# 3. 检查环境变量
# 确认 WECHAT_CALLBACK_TOKEN 已配置
```

---

### Q3: 关注公众号后未收到事件推送

**可能原因**:
1. 服务器配置未启用
2. 用户没有unionid（未登录过小程序）
3. 事件处理代码有bug

**排查步骤**:
```bash
# 1. 检查云托管日志，搜索关键词 "subscribe"
# 2. 查询用户unionid
npx prisma studio
# 3. 使用诊断工具
node /tmp/fix-user-subscribe.js diagnose <userId>
```

---

### Q4: 模板消息发送失败，报错 "invalid openid"

**原因**: 使用了小程序openid，应该使用公众号openid（mpOpenid）

**检查代码**: `app/api/reports/[id]/route.ts:80`
```typescript
// ❌ 错误
mpOpenid: currentReport.user!.openid

// ✅ 正确
mpOpenid: currentReport.user!.mpOpenid!
```

---

## 完成检查清单

部署完成后，逐项检查：

- [ ] 订阅号已升级为服务号
- [ ] 服务号已完成认证
- [ ] 已购买并备案域名
- [ ] 域名已绑定到云托管并验证成功
- [ ] 微信公众号服务器配置已启用
- [ ] 测试关注事件推送成功
- [ ] 测试取消关注事件推送成功
- [ ] 测试报告发布推送成功
- [ ] 点击公众号消息可跳转小程序

---

## 总耗时估算

| 步骤 | 预估时间 |
|------|---------|
| 升级到服务号 | 3-7天（人工审核）|
| 购买域名 | 10分钟 |
| 域名备案（如未备案）| 7-20天 |
| 绑定域名到云托管 | 30分钟 |
| 配置微信服务器 | 10分钟 |
| 测试验证 | 1小时 |

**总计**: 如果域名已备案，约1-2小时完成配置；如果需要备案，约10-30天。

---

## 下一步操作

请告诉我你的域名情况：

1. **已有备案域名** → 告诉我域名，我帮你生成完整配置命令
2. **有域名但未备案** → 需要先完成备案（7-20天）
3. **还没有域名** → 推荐购买 `.com` 或 `.cn` 域名并备案

同时，请开始**升级到服务号**的流程，这个审核需要3-7天。
