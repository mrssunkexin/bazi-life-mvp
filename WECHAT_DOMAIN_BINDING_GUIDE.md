# 微信云托管绑定自定义域名指南

## 问题说明

微信公众平台不允许使用 `.tcloudbase.com` 测试域名配置服务器回调接口。
错误提示：**云托管域名仅用作测试使用，不可用在正式环境下**

**前置条件**：
- ✅ 已将订阅号升级为服务号（或直接申请服务号）
- ✅ 服务号已完成认证
- ⚠️ 需要准备一个已备案的域名

## 解决方案：绑定自定义域名

### 步骤1：准备域名

你需要一个已备案的域名，例如：
- `api.yourdomain.com`
- `wechat.yourdomain.com`

### 步骤2：在微信云托管控制台绑定域名

1. 登录 [微信云托管控制台](https://cloud.weixin.qq.com)
2. 进入你的服务 `zhibaitang-bazisever-207188-4-1391586262`
3. 找到 **"访问配置"** 或 **"域名管理"** 菜单
4. 点击 **"添加域名"** 或 **"绑定自定义域名"**
5. 输入你的域名，例如 `api.yourdomain.com`
6. 按照提示添加 DNS 解析记录（通常是 CNAME 记录）

### 步骤3：DNS 解析配置

在你的域名服务商（阿里云/腾讯云等）添加 CNAME 记录：

```
主机记录: api（或你选择的子域名）
记录类型: CNAME
记录值: zhibaitang-bazisever-207188-4-1391586262.sh.run.tcloudbase.com
TTL: 600
```

### 步骤4：等待生效

- DNS 解析生效通常需要 10-30 分钟
- 云托管平台会自动验证并启用域名

### 步骤5：更新微信公众号配置

域名绑定成功后，使用新域名配置微信回调：

```
服务器地址(URL): https://api.yourdomain.com/api/wechat/callback
Token: bazi_life_2025_secure_token
EncodingAESKey: (已生成的值)
消息加解密方式: 明文模式
数据格式: XML
```

---

## 方案2：暂时使用明文模式（如果绑定域名有困难）

如果暂时无法绑定自定义域名，可以考虑：

### 选项A：联系微信支持

某些情况下，微信可能允许测试账号使用云托管域名，可以尝试：
1. 在公众平台提交工单
2. 说明你的情况（开发测试阶段）
3. 申请临时使用测试域名

### 选项B：使用手动绑定工具

暂时使用已创建的诊断工具手动绑定用户：

```bash
# 1. 获取用户的公众号 openid（需要用户发送消息给公众号）
# 2. 使用工具手动设置关注状态
node /tmp/fix-user-subscribe.js set <userId> <mpOpenid>
```

**注意**: 这种方式无法自动接收关注/取消关注事件，需要手动维护

---

## 推荐方案

**强烈建议使用方案1（绑定自定义域名）**，原因：
1. 符合微信公众平台规范
2. 可以正常接收所有事件推送
3. 长期稳定可靠
4. 用户体验更好

域名可以在阿里云、腾讯云等平台购买，费用通常为几十元/年。

---

## 验证域名绑定成功

绑定完成后，使用以下命令测试新域名：

```bash
# 替换为你的自定义域名
curl "https://api.yourdomain.com/api/wechat/callback?signature=test&timestamp=1234567890&nonce=test&echostr=hello"
```

如果返回 "Invalid signature"（而不是500错误），说明域名绑定成功。

然后运行完整测试脚本：

```bash
# 修改 test-deployment.sh 中的 DOMAIN 变量
DOMAIN="https://api.yourdomain.com"

# 运行测试
bash test-deployment.sh
```

---

## 常见问题

**Q: 我没有域名怎么办？**
A: 可以在阿里云、腾讯云等平台购买，.com 域名约50-80元/年，.cn 域名约20-40元/年。

**Q: 域名需要备案吗？**
A: 在中国大陆使用的域名必须完成ICP备案。备案流程通常需要7-20天。

**Q: 可以使用二级域名吗？**
A: 可以，推荐使用二级域名，如 `api.yourdomain.com` 或 `wechat.yourdomain.com`。

**Q: 绑定域名后原来的测试域名还能用吗？**
A: 可以，两个域名会同时有效，但微信回调只能配置一个URL。

---

## 下一步

请告诉我你选择哪个方案：
1. **绑定自定义域名**（我可以帮你检查DNS配置）
2. **联系微信支持**申请测试权限
3. **暂时使用手动绑定工具**（功能受限）

如果你有域名，请告诉我域名名称，我可以帮你生成具体的配置步骤。
