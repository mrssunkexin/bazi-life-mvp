一、黄历首页需求（只写本期要做的）

本需求仅描述 老黄历-首页
页面结构以你提供的原型图为准
本文档只定义字段、规则、算法要求与交互

1. 页面名称

老黄历 · 首页

2. 顶部区域
2.1 标题

固定标题：老黄历

2.2 右上角

日历图标（占位）

点击行为：toast 提示「功能暂未开放」

3. 日期主信息区（核心交互）
3.1 展示内容

公历日期：YYYY年M月D日 星期X

农历日期：农历月日（如：十一月初八）

大号日期数字：D

标签（Tag）：

属相

第 X 周（公历周）

节令标签（数九 / 三伏，是否显示由算法决定）

3.2 日期切换

支持左右滑动切换日期

左滑：下一天

右滑：上一天

切换后：

当前日期状态更新

页面所有黄历数据重新计算并刷新

使用轻量 loading（不整页白屏）

4. 节令 / 节气 / 节日信息
4.1 节令标签（数九 / 三伏）

是否展示、展示内容 完全由算法计算结果决定

若算法判断当前日期属于：

数九期间：显示对应「一九 ~ 九九」

三伏期间：显示「初伏 / 中伏 / 末伏」

其他日期：不显示节令标签

⚠️ 数九、三伏的判定规则
由 Claude Code 自行在 GitHub 查找成熟、可验证的开源算法实现

4.2 二十四节气

展示两行：

上一个节气：节气名 + 精确时间

下一个节气：节气名 + 精确时间

时间精度：到秒

节气计算必须来自成熟开源算法

4.3 农历节日提示（仅农历节日）

使用中国传统农历节日

展示规则：

若当天是节日：显示
今天是 ××

若当天不是节日：显示
距离下一个农历节日 ×× 还有 X 天

农历换算与节日判断需支持闰月

5. 宜 / 忌（核心模块）
5.1 展示规则

左列：宜

右列：忌

展示算法返回的 全部条目

不折叠、不省略、不限制数量

5.2 算法要求

使用专业老黄历 / 通胜类开源算法

宜忌、冲煞、时辰、值日星神等字段应来自同一算法体系

禁止自行拼规则

6. 冲煞信息
展示格式
冲 {生肖}（{干支}） 煞{方位}


所有字段由算法直接给出

7. 中部信息卡（与原型一致）
7.1 流年 / 流月 / 流日

展示格式：
{年干支}年 {月干支}月 {日干支}日

7.2 五行

展示年 / 月 / 日对应五行或纳音

以算法输出为准

7.3 值日星神

展示：
{星神名}（黄道 / 黑道）

次行：
吉 / 凶 / 平（完全由算法决定）

8. 吉神方位

展示字段：

财神位

喜神位

福神位

阳贵神

阴贵神

展示形式：字段名 + 方位

9. 时辰吉凶（12 时辰）
9.1 展示内容

每个时辰块包含：

时辰名

时间段

吉 / 凶 / 平（以算法结果为准）

9.2 当前时辰

根据用户本地时间判断

当前时辰高亮显示

9.3 交互

点击时辰：本期不做任何弹层或跳转

10. 算法总要求（只说一次）

以下数据 必须使用 GitHub 上成熟开源算法实现：

农历（含闰月）

二十四节气（含精确时间）

数九

三伏

农历节日

宜 / 忌

冲煞

干支

五行

值日星神

时辰吉凶

要求 Claude Code 在选型后，用 2025-12-27 对齐主流老黄历结果进行验证。

11. 关于原型样式的说明（重要）

当前原型为 功能原型，
配色与视觉存在不合理之处，允许在不改变信息结构的前提下进行视觉优化。
不要求像素级还原。

二、小程序整体结构需求（新增）
1. 小程序底部 Tab 结构

小程序采用 底部三栏 Tab：

[ 黄历 ]  [ 运势 ]  [ 我的 ]

2. 各 Tab 职责说明
2.1 黄历（本期重点）

页面：老黄历-首页

功能：展示每日老黄历信息

本期：只实现首页

2.2 运势（复用已有页面）

运势 Tab 不新开发页面

直接复用当前小程序中 已存在的运势页面

工程要求（非常关键，避免 Claude 误解）

运势 Tab 的实现方式：

在 Tab 切换时

直接跳转 / 挂载 / 引用 现有运势页面

不修改运势页面内部逻辑

不重写 UI

不复制代码

仅作为入口位置调整

本质：
“把现在已有的一个页面，放到 TabBar 的运势位置”

2.3 我的（预留）

仅作为占位 Tab

本期可展示简单占位文案：

「功能开发中」

不实现具体功能

3. TabBar 实现要求

使用微信小程序原生 TabBar 或自定义 TabBar 均可

Tab 切换时页面状态保持（不强制重建）


2025-12-30 17：00 新增
时辰五行能量模块需求
一、模块定位

在黄历首页「时辰吉凶」模块下方，新增模块：

模块名称：时辰五行能量

用于展示：

当天 流日的天干、地支，以及地支藏干五行能量

每个时辰的 时干、时支，以及地支藏干五行能量

二、流日五行能量（顶部总览）
1）五行颜色标注（图例，最上方）

在模块顶部展示一行「五行颜色标注」

内容：5 个色块 + 文字标签
木 / 火 / 土 / 金 / 水

该行仅用于说明颜色含义，不表示比例、不表示能量

2）流日五行能量（顶部总览）

流日区域从左到右展示 3 块：

2.1 流日天干（标识）

显示：日干（如：己）

仅显示文字

背景色按该天干所属五行着色

不显示比例

2.2 流日地支（标识）

显示：日支（如：亥）

仅显示文字

背景色按该地支本气五行着色

不显示比例

2.3 流日地支藏干能量条（核心，100%）

展示一条横向能量条，总长度代表 100%

能量条由 日支的藏干 构成（1~3 段）

每段代表一个藏干，并展示：

藏干文字（如：壬）

百分比（如：70%）

背景色按藏干所属五行着色

各段百分比加总必须为 100%

藏干百分比分配规则（固定写死）

1 个藏干：100%

2 个藏干：主气 70%，中气 30%

3 个藏干：主气 60%，中气 30%，余气 10%

百分比不从库读取；库只负责给出“藏干列表及主/中/余顺序”

3）各时辰五行能量（12 行明细）

每个时辰一行，展示：

3.1 时辰信息

时辰名（子丑寅…）+ 时间段（23:00–00:59）

3.2 时辰天干（标识）

显示：时干

背景色按天干五行着色

不显示比例

3.3 时辰地支（标识）

显示：时支

背景色按地支本气五行着色

不显示比例

3.4 时辰地支藏干能量条（核心，100%）

一条能量条，总长度 100%

由该时支藏干组成（1~3 段）

每段显示：藏干文字 + 百分比

百分比按上面的固定规则分配（1藏=100，2藏=70/30，3藏=60/30/10）

加总必须为 100%


2.3 能量口径说明（强约束）

能量条只计算地支藏干

天干、地支本身只用于标识五行属性，不参与比例计算

藏干比例之和 必须等于 100%

三、时辰五行能量明细（12 行）
3.1 每个时辰一行，包含以下信息（从左到右）
1）时辰信息

时辰名（子、丑、寅……）

时间段（如 23:00–00:59）

2）时辰天干

显示：时干

仅显示文字

背景色按该天干五行着色

不显示比例

3）时辰地支

显示：时支

仅显示文字

背景色按该地支本气五行着色

不显示比例

4）地支藏干能量条

展示方式与「流日藏干能量条」一致

能量条由 1–3 段组成：

每段显示：藏干文字 + 百分比

百分比按 固定比例规则

各段比例之和 必须等于 100%

四、算法与数据来源要求（明确分工）
4.1 必须由算法库提供的内容（去 GitHub 查）

Claude Code 必须使用成熟开源算法库 来计算以下内容：

日柱干支复用现在的流日算法，避免出现和上面流日干支不一样情况

时柱干支复用现在的流时算法，避免出现和上面流日干支不一样情况


地支对应的藏干列表（含主气 / 中气 / 余气顺序）

天干 → 五行映射

地支 → 本气五行映射

藏干 → 五行映射

4.2 不需要算法库参与的内容

藏干能量比例
→ 严格使用本需求中定义的固定比例规则

五、数据一致性与校验

任意一条能量条（流日 / 单个时辰）：

藏干比例总和必须为 100%

不允许出现：

比例缺失

比例超过或小于 100%

若算法库返回异常（缺失藏干）：

该行不渲染能量条

页面其他内容正常显示

六、交互规则

本模块为纯展示模块

不支持点击

不支持弹窗

不支持解释页

随日期左右切换同步刷新

七、样式说明（唯一一句）

原型为功能原型，配色不作为最终标准；在不改变信息结构的前提下，允许调整为更协调、低饱和的配色。


2025 年 12 月 31 日 16:09:31

【查看更多详细信息】＋【兑换码解锁（日历会员年卡 / 终身卡）】最终实现需求

本需求用于直接指导实现，不包含解释性文字，不包含未做功能。

一、整体目标

在老黄历首页增加一个入口【查看更多详细信息】。

未解锁用户：只能看到基础内容，需要通过兑换码解锁

已解锁用户：直接看到页面全部内容，不再显示兑换入口

解锁状态 绑定微信 openid

同一 openid 后续登录 自动解锁，无需重复兑换

二、前端（小程序）实现需求
1. 入口按钮

位置：老黄历首页指定位置

文案：查看更多详细信息

显示规则

当 openid 未解锁日历会员：

显示该按钮

页面仅展示基础内容

当 openid 已解锁日历会员（年卡未过期 / 终身卡）：

隐藏该按钮

直接展示页面全部内容

2. 点击入口后的行为（兑换弹窗）

点击【查看更多详细信息】后：

在当前黄历页弹出兑换弹窗

不跳转页面

不改变当前日期状态

弹窗结构

标题：兑换会员

输入框：

placeholder：请输入兑换码

按钮：

确认兑换

关闭方式：

右上角关闭 / 点击遮罩

3. 弹窗关闭规则

如果用户：

关闭弹窗

或兑换失败
→ 保持当前页面状态不变（仍为未解锁，仅基础内容）

如果兑换成功
→ 立即解锁页面全部内容（见第 4 点）

4. 兑换成功后的前端行为（必须）

当兑换接口返回成功时，前端必须立即执行以下逻辑：

关闭兑换弹窗

将当前用户状态更新为「已解锁日历会员」

隐藏【查看更多详细信息】按钮

立即展示当前黄历页的全部内容

不需要刷新页面

不需要重新进入页面

当前日期保持不变

5. 页面初始化时的解锁校验

进入老黄历首页时：

使用当前登录态获取 openid

调用后端接口查询该 openid 的日历会员状态

若返回「已解锁」：

默认展示全部内容

不显示兑换入口

若返回「未解锁 / 已过期」：

展示兑换入口

仅展示基础内容

三、兑换码类型定义（后端）

在原有兑换码系统基础上，新增两个兑换码类型：

1. 兑换码类型

fortune：运势测算（原有，不改）

calendar_year：日历会员年卡

calendar_life：日历会员终身卡

2. 兑换码生成规则

年卡 / 终身卡兑换码：

随机生成

与现有兑换码规则一致

生成时写入对应 type

后台兑换码管理页面：

可区分显示兑换码类型

年卡 / 终身卡在列表中可识别

四、兑换与会员规则（必须写死）
1. 兑换绑定规则

日历会员兑换 必须绑定微信 openid

后台必须记录：

openid

使用的兑换码 code

兑换码类型（年卡 / 终身卡）

兑换时间

2. 年卡规则（已确认）

年卡有效期：

从兑换成功时间起算 365 天

数据表现：

expire_at = redeemed_at + 365 天

3. 终身卡规则

终身卡：

无过期时间

expire_at = null

4. 升级规则（已确认）

若某 openid 已是年卡会员，再次兑换：

终身卡兑换码
→ 允许升级为终身卡

升级行为：

覆盖原会员类型为 calendar_life

更新 redeemed_code

保留历史兑换记录

五、兑换接口（后端）
1. 兑换接口

POST /api/calendar/redeem

请求参数：

{
  "openid": "USER_OPENID",
  "code": "EXCHANGE_CODE"
}

校验逻辑（顺序必须一致）

校验兑换码是否存在

校验兑换码是否未使用

校验兑换码类型是否为 calendar_year 或 calendar_life

标记兑换码为已使用：

写入 used_openid

写入 used_at

创建或更新日历会员记录：

年卡：写入 expire_at

终身卡：expire_at = null

返回兑换成功

成功返回示例：

{
  "success": true,
  "membership_type": "calendar_year",
  "expire_at": "2026-12-31T10:30:00"
}


失败返回示例：

{
  "success": false,
  "error_code": "CODE_USED",
  "message": "兑换码已被使用"
}

2. 查询会员状态接口

GET /api/calendar/membership?openid=USER_OPENID

返回示例：

未开通 / 已过期：

{
  "is_member": false
}


年卡有效：

{
  "is_member": true,
  "membership_type": "calendar_year",
  "expire_at": "2026-12-31T10:30:00"
}


终身卡：

{
  "is_member": true,
  "membership_type": "calendar_life",
  "expire_at": null
}

六、前端错误提示规范

根据接口返回错误类型，前端提示文案如下：

兑换码不存在：兑换码无效

兑换码已使用：兑换码已被使用

兑换码类型不支持：兑换码类型不支持

系统异常：兑换失败，请稍后重试

七、关键约束（必须遵守）

解锁状态只以 openid 为准

同一 openid：

只要存在有效日历会员记录

即可直接查看全部页面内容

页面解锁：

不跳转

不刷新整页

直接在当前页面状态下展示