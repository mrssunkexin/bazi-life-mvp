# 八字分析小程序 + 后台管理系统 PRD（V1）

文档版本：V1.0  
撰写日期：2025-12-08  
产品负责人：花隐  
适用范围：微信小程序、管理后台 Web、后端接口开发

---

## 0. 名词约定

- **用户**：使用小程序提交信息、查看报告的人  
- **管理员 / 分析师**：后台写报告的人  
- **报告**：基于用户出生信息生成的八字/五行分析文本  
- **兑换码**：用户在其他平台购买后获得的激活码，用于生成报告  
- **服务号**：用于通知用户“报告已完成”的微信服务号  
- **小程序**：本项目的用户端入口  

---

## 1. 项目概述

### 1.1 项目背景

用户在小红书、抖音等平台购买“八字/五行分析服务”后，通过兑换码在小程序提交出生信息，后台由人工分析后生成报告，通过服务号通知用户查看。

### 1.2 项目目标（V1）

- 为用户提供流畅的：提交信息 → 等待分析 → 查看报告 流程  
- 为管理员提供高效的：查看提交 → 编辑报告 → 发布报告 → 推送通知 流程  
- 支持兑换码激活机制，统计有多少人提交但未激活（沉没成本 + 转化漏斗）

### 1.3 不在本期范围（V1 排除）

- 小程序内直接支付（支付能力）  
- AI 自动生成报告  
- 多分析师协作、权限分级  
- 数据看板 / BI 报表  

---

## 2. 用户与角色

### 2.1 用户角色

1. **小程序用户**
   - 通过小程序提交生日等信息
   - 使用兑换码激活报告
   - 查看已生成的报告
   - 可查看历史报告

2. **管理员 / 分析师**
   - 使用 Web 后台登录
   - 查看待分析记录
   - 编辑、保存、发布报告
   - 批量生成、管理兑换码

3. **系统（服务号 + 后端）**
   - 负责登录态、数据存储
   - 负责在报告发布后向用户发送服务号通知（后续打通）

---

## 3. 业务流程（整体）

### 3.1 用户端主流程

1. 用户打开小程序（来自服务号菜单或扫码等）  
2. 进入首页，点击【开始填写信息】  
3. 在信息填写页填写姓名、性别、出生日期/时间、城市、备注，可选填写兑换码  
4. 点击【生成报告】  
5. 系统创建一条“报告记录”，判断是否有有效兑换码：  
   - 有效 → 状态为待分析，进入“正在分析中”页  
   - 无兑换码 / 兑换码无效 → 弹窗提示需激活（填兑换码 or 未来付费），记录为待激活  
6. 管理员在后台查看“待分析”记录，完成报告编辑并发布  
7. 报告发布后，系统通过服务号推送通知（后续打通）  
8. 用户在小程序中查看报告（可从通知跳转或历史报告中查看）

### 3.2 管理端主流程

1. 管理员打开后台 Web，账号密码登录  
2. 在【报告列表】中查看“待分析”状态的记录  
3. 点击进入【报告编辑页】，基于用户出生信息和八字排盘填写报告内容  
4. 保存草稿或直接发布  
5. 发布时：  
   - 更新报告状态为 published  
   - 记录发布时间  
   - 调用服务号发送报告完成通知（若已打通）

### 3.3 兑换码使用流程（概要）

1. 管理员在后台批量生成兑换码，一般用于在其他平台售卖  
2. 用户购买后获得兑换码  
3. 用户在小程序填写信息时，可选择填写兑换码  
4. 提交后，系统校验兑换码：  
   - 正确且未使用 → 绑定到该用户 + 该报告  
   - 错误或已使用 → 提示“不可用”，记录为 code_invalid  
5. 未输入兑换码的用户 → 记录为 pending_pay，后续可统计“提交但未激活”的人数

---

## 4. 小程序端需求（详细）

### 4.1 小程序整体要求

- 使用微信小程序原生框架
- 所有接口调用前必须确保用户已通过 wx.login 完成后台登录（获取 token）
- 操作体验尽量减少页面跳转，缩短路径，重要拦截放在“提交之后”

---

### 4.2 页面与功能

#### 4.2.1 首页 `/pages/home`

**功能目标：**
- 用最少信息引导用户进入填写流程
- 提供历史报告入口

**内容结构：**
- 顶部：项目标题 + 简短描述（如“基于八字的个性与五行分析报告”）  
- 主按钮：【开始填写信息】 → 跳转 `/pages/form`  
- 次按钮：【查看历史报告】 → 跳转 `/pages/history`  
- 底部：  
  - 【隐私协议】  
  - 【用户协议】

**交互要求：**
- 不做任何校验和拦截
- 不展示兑换码入口（兑换码在填写页内）

---

#### 4.2.2 信息填写页 `/pages/form`

**功能目标：**
- 收集生成报告所需的用户信息
- 作为“沉没成本”页面，提交后再拦截兑换逻辑

**字段需求：**

| 字段名       | 类型         | 是否必填 | 说明                     |
|--------------|--------------|----------|--------------------------|
| 姓名         | 文本         | 否       | 可留空                   |
| 性别         | 单选         | 否       | 男 / 女 / 其他，可空    |
| 出生日期     | 日期选择器   | 是       | yyyy-MM-dd              |
| 出生时间     | 时间选择器   | 是       | HH:mm                   |
| 出生城市     | 文本或选择器 | 是       | 城市名                   |
| 备注         | 多行文本     | 否       | 用户给分析师的补充说明  |
| 兑换码       | 文本输入框   | 否       | 用户可直接输入兑换码    |

**按钮：**
- 底部主按钮：【生成报告】

**提交逻辑（核心）：**

点击【生成报告】后：

1. 前端校验必填项  
2. 调用后端接口 `POST /api/report/create-temp`，请求体包括所有表单字段 + 可选的 `redeemCode`  
3. 后端返回：
   - `reportId`
   - `status`（`pending_analysis` / `pending_pay`）
   - `codeStatus`（`valid` / `invalid` / `none`）
   - `needPay`（bool）
   - `needCode`（bool）

**前端根据返回结果的处理：**

- 情况 A：`status = pending_analysis` 且 `codeStatus = valid`  
  - 说明兑换码有效，报告已进入“待分析”队列  
  - 跳转至 `/pages/pending?reportId=xxx`  
  - 同时弹出浮层引导关注服务号（文案后面写）

- 情况 B：`status = pending_pay` 且 `codeStatus = none`  
  - 用户未输入兑换码  
  - 弹出弹窗提示：

    > 您的信息已提交，为生成完整分析报告，需要激活权限：  
    > - 输入兑换码  
    > - 或购买完整版报告（后续上线）  

  - 按钮：
    - 【输入兑换码】 → 留在当前页，焦点聚焦到兑换码输入框，用户再输入并调用 `/api/report/apply-redeem`
    - 【暂不激活】 → 关闭弹窗，用户仍可离开；后端保留 pending_pay 状态记录（用于统计）

- 情况 C：`codeStatus = invalid`  
  - 弹窗提示：

    > 您的兑换码不可用，请检查后重新输入。

  - 保留报告记录为 pending_pay，记录用户行为（code_invalid）

**埋点/统计需求：**

在以下节点上报（给后端 funnel_logs 写入）：

- 表单完成并点击提交：step = `form_submitted`  
- 后端返回 pending_pay & codeStatus = none：step = `pending_pay_no_code`  
- 用户输入兑换码并提交：step = `code_entered`  
- 兑换码无效：step = `code_invalid`  
- 兑换码有效：step = `code_valid`

---

#### 4.2.3 正在分析页 `/pages/pending`

**入口：**
- 信息填写页 → 提交成功且兑换码有效（pending_analysis）

**展示内容：**
- 标题：如“报告已提交，分析中…”  
- 显示：  
  - 报告编号（reportId）  
  - 提交时间  
- 文案提示：  
  - 报告由老师人工撰写，一般 xx 小时内完成（具体时间可后端配置）  

**关注服务号引导：**

在页面中部或底部展示一个醒目模块：

> 为第一时间收到“报告已完成”的提醒，建议关注服务号。  
> [前往关注服务号] 按钮

按钮点击后跳转至服务号关注页面（技术上由后端/小程序配置）。

**可选功能：**
- 轮询报告状态（每 30s 调用一次类似 `GET /api/report/:id`）  
- 如果状态变为 published，可给予提示：“报告已完成，是否立即查看？”

---

#### 4.2.4 报告详情页 `/pages/report`

**入口：**

- 服务号模板消息跳转  
- 历史报告列表点击  
- 用户收藏或记录的链接

**展示内容：**

1. 报告头部信息：
   - 姓名（如填写）
   - 性别
   - 出生日期、时间
   - 出生城市
   - 报告生成时间

2. 八字基础信息：
   - 年柱、月柱、日柱、时柱（文字展示）  
   - 备注：具体格式由后台生成内容决定

3. 五行统计（可选）：
   - 表格或简单条形图（如“木：XX%、火：XX%”）

4. 报告正文：
   - 采用 Markdown 渲染  
   - 按段落展示，例如：  
     - 总体概况  
     - 性格特质  
     - 事业方向  
     - 感情倾向  
     - 健康注意  
     - 总结与建议  

5. 底部按钮：
   - 【返回首页】  
   - 【查看历史报告】  

**异常情况：**
- 报告尚未发布（状态不是 published）：
  - 提示：“报告仍在分析中，请稍后再查看。”

---

#### 4.2.5 历史报告页 `/pages/history`

**功能：**
- 展示当前用户所有报告列表（已提交的）  
- 按创建时间倒序排序

**列表字段：**
- 报告标题（如：2026 流年分析 / 生辰五行分析 等，可由后台字段或自动生成）  
- 创建时间  
- 状态（待分析 / 已发布）  

**交互：**
- 点击一条记录 → 跳转 `/pages/report?reportId=xxx`

---

#### 4.2.6 设置页 `/pages/settings`（可简化）

**内容：**
- 【隐私协议】  
- 【用户协议】  
- 【联系客服 / 关注服务号】入口  
- 【绑定手机号】（后期可用）

---

## 5. 管理后台需求（详细）

后台是管理员/分析师使用的 Web 管理系统。

### 5.1 登录页

**字段：**
- 用户名
- 密码

**流程：**
- 输入正确账号密码 → 登录成功，进入报告列表页  
- 密码错误 → 提示“账号或密码错误”  

（找回密码功能可暂不实现）

---

### 5.2 报告列表页

**入口：** 登录后默认进入

**筛选条件：**
- 状态：  
  - 待分析（pending_analysis）  
  - 草稿（draft）  
  - 已发布（published）  
  - 待激活（pending_pay，可选）  
- 创建时间范围  
- 姓名（模糊搜索）  
- 城市（可选）

**列表字段：**

| 字段       | 说明                       |
|------------|----------------------------|
| 报告ID     | 唯一标识                   |
| 姓名       | 若为空则显示“未填写”       |
| 性别       |                            |
| 出生日期   |                            |
| 出生时间   |                            |
| 出生城市   |                            |
| 状态       | 待分析/草稿/已发布等       |
| 创建时间   | 用户提交时间               |
| 操作       | 查看 / 编辑 / 复制链接等   |

**操作需求：**
- 点击一行 → 进入报告详情/编辑页  
- 列表支持分页（默认 20 条/页）

---

### 5.3 报告编辑页

**目标：**
- 让管理员在同一页面看到用户信息+八字信息，并编辑报告正文

**页面布局（推荐）：**
- 左侧：用户基础信息 + 八字排盘（仅展示，不编辑）  
- 右侧：  
  - Markdown 编辑区  
  - 预览区（Tab 切换）

**左侧内容：**
- 姓名 / 性别  
- 出生日期 / 时间  
- 出生城市  
- 备注（用户写的）  
- 八字排盘信息：年柱、月柱、日柱、时柱、地支藏干等（如实现）  

**右侧内容：**

- Markdown 编辑区域，建议支持：
  - 标题（#、##）  
  - 加粗、列表  
  - 基本文字格式  

**按钮：**
- 【保存草稿】  
  - 状态置为 draft  
  - 不触发通知

- 【发布报告】  
  - 状态置为 published  
  - 写入发布时间  
  - 触发服务号通知（如打通）

**边界情况：**
- 已发布报告再次编辑：  
  - 允许修改内容并重新保存（可不再触发通知，视产品决定）  

---

### 5.4 报告详情页（只读）

- 与编辑页类似，但均为只读模式  
- 通常用于查看历史记录，不进行编辑

---

### 5.5 兑换码管理

#### 5.5.1 批量生成兑换码

**入口：** 【兑换码管理】 → 【生成兑换码】

**输入项：**
- 数量（必填，如 50, 100, 500）  
- 备注（可选，如“2026春节活动礼包”）  
- 有效期（可选，设定到期日期）

**输出：**
- 列表展示生成的兑换码，可复制/导出 Excel  
- 每个兑换码默认状态：unused

---

#### 5.5.2 兑换码列表

**字段：**

| 字段         | 说明                           |
|--------------|--------------------------------|
| 兑换码       | 唯一字符串                     |
| 状态         | 未使用/已使用/已禁用/已过期    |
| 使用者       | 用户ID + 姓名（若有）         |
| 关联报告ID   | 使用该码生成的报告            |
| 使用时间     | 使用时的时间                   |
| 批次号       | 用于区分不同生成批次          |
| 备注         | 管理员填写的说明              |

**功能：**
- 按状态筛选  
- 按批次筛选  
- 导出 Excel  
- 选中某条兑换码 → 可执行【禁用】（将该码设为 disabled）

---

### 5.6 用户管理（简单版）

**列表字段：**

| 字段        | 说明                      |
|-------------|---------------------------|
| 用户ID      | 唯一标识                 |
| 手机号      | 若已绑定则展示           |
| 报告数量    | 该用户提交/生成的报告数  |
| 最近活跃时间| 最近一次访问/操作时间    |

**操作：**
- 点击某用户 → 进入用户详情，查看该用户的报告列表

---

## 6. 数据 & 状态（产品视角）

### 6.1 报告状态

- `pending_pay`：用户提交表单，但未激活（无兑换码/兑换码无效）  
- `pending_analysis`：已激活，待分析师撰写报告  
- `draft`：分析师已填写部分内容，未发布  
- `published`：报告已完成并对用户可见  
- `cancelled`（预留）：如为退款或作废情况预留

### 6.2 兑换码状态

- `unused`：未使用  
- `used`：已使用并绑定在某个 report + user 上  
- `expired`：已过期  
- `disabled`：被管理员手动禁用

---

## 7. 数据统计 & 埋点需求（产品视角）

**目的：** 统计漏斗各环节人数，为优化转化提供依据。

关键节点：

1. 打开填写页  
2. 点击【生成报告】  
3. 后端返回 pending_pay + codeStatus = none → 提交但无兑换码  
4. 用户输入兑换码次数  
5. 兑换码无效次数  
6. 兑换码有效次数  
7. 最终进入 pending_analysis 的人数  
8. 最终报告发布人数（即成功完成服务人数）

这些可通过：

- `funnel_logs` 表记录 step + user + report  
- 报告状态统计实现

---

## 8. 文案 & 校验规则（示例）

### 8.1 错误提示文案示例

- 出生日期未填写：  
  > 请选择您的出生日期

- 兑换码无效：  
  > 您的兑换码不可用，请检查后重新输入

- 报告未发布：  
  > 报告仍在分析中，请稍后再查看

### 8.2 字段校验规则（简版）

- 出生日期：必填，需为合法日期  
- 出生时间：必填，可用“未知”按钮（若允许）  
- 城市：建议长度 2–20 字符  
- 备注：最长建议 200–500 字  
- 兑换码：长度控制在 6–32 字符，字母数字组合

---

## 9. 版本规划（简要）

### V1（本 PRD 范围）

- 小程序端：提交信息、兑换码激活、查看报告、历史报告  
- 后台端：报告列表、编辑、发布、兑换码管理、简单用户管理  
- 服务号通知：结构预留，具体接入可在 V1.1 实现

### V2（规划）

- 支付接入（合规前提下）  
- 报告生成海报分享  
- 手机号登录 & 找回报告  
- 更细的用户画像与标签

---

_本 PRD 旨在达到“开发可以直接按此实现”的细致程度，如后续开发或测试环节发现有遗漏，可在 V1.1 文档中补充修订。_
